// Enum para diferentes tamanhos de texto
enum TextSizes { small, medium, large }

// Enum para diferentes status de pedido
enum OrderStatus { processing, shipped, delivered }

// Enum para diferentes métodos de pagamento
enum PaymentMethod { 
  paypal, googlePay, applePay, visa, masterCard, creditCard, 
  paystack, razorPay, paytm 
}